<x-layouts.app title="Auditoría">
<div><x-ui.badge variant="info">Control institucional</x-ui.badge><h1 class="mt-3 text-2xl font-bold text-navy-900">Auditoría y descargas</h1><p class="mt-2 text-sm text-slate-600">Bitácora inalterable de acciones y acceso a reportes.</p></div>
<x-ui.card class="mt-6"><form method="GET" class="grid gap-4 md:grid-cols-2 xl:grid-cols-5"><div><label class="mb-1 block text-xs font-bold text-slate-500">Acción</label><select name="accion" class="min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm"><option value="">Todas</option>@foreach($actions as $action)<option @selected(request('accion')===$action)>{{ $action }}</option>@endforeach</select></div><div><label class="mb-1 block text-xs font-bold text-slate-500">Usuario</label><select name="user_id" class="min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm"><option value="">Todos</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected((string)request('user_id')===(string)$user->id)>{{ $user->name }}</option>@endforeach</select></div><x-ui.form-field label="Desde" name="desde" type="date" :value="request('desde')"/><x-ui.form-field label="Hasta" name="hasta" type="date" :value="request('hasta')"/><div class="flex items-end gap-2"><button class="app-button-primary">Filtrar</button><a href="{{ route('audit.index') }}" class="app-button-secondary">Limpiar</a></div></form></x-ui.card>
<x-ui.card class="mt-6" :padding="false"><div class="border-b border-slate-100 p-5"><h2 class="text-lg font-bold text-navy-900">Bitácora de cambios</h2></div><div class="overflow-x-auto"><table class="w-full min-w-[65rem] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Fecha</th><th class="px-4 py-3">Usuario</th><th class="px-4 py-3">Acción</th><th class="px-4 py-3">Recurso</th><th class="px-4 py-3">Origen</th><th class="px-4 py-3">Detalle</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($audits as $audit)<tr><td class="px-4 py-3 whitespace-nowrap">{{ $audit->created_at->format('d/m/Y H:i:s') }}</td><td class="px-4 py-3">{{ $audit->usuario?->name ?? 'Sistema' }}</td><td class="px-4 py-3 font-semibold text-slate-800">{{ $audit->accion }}</td><td class="px-4 py-3">{{ $audit->tabla }} #{{ $audit->registro_id }}</td><td class="px-4 py-3">{{ $audit->ip_address ?? 'Interno' }}</td><td class="px-4 py-3"><details><summary class="cursor-pointer font-semibold text-brand-700">Ver cambios</summary><pre class="mt-2 max-w-xl whitespace-pre-wrap rounded-lg bg-slate-950 p-3 text-xs text-slate-100">{{ json_encode(['antes'=>$audit->valores_anteriores,'después'=>$audit->valores_nuevos], JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></details></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No existen registros para los filtros seleccionados.</td></tr>@endforelse</tbody></table></div>@if($audits->hasPages())<div class="border-t border-slate-100 p-4">{{ $audits->links() }}</div>@endif</x-ui.card>
<x-ui.card class="mt-6" :padding="false"><div class="border-b border-slate-100 p-5"><h2 class="text-lg font-bold text-navy-900">Descargas recientes de reportes</h2></div><div class="divide-y divide-slate-100">@forelse($downloads as $download)<div class="flex flex-wrap justify-between gap-3 p-5"><div><p class="font-semibold text-slate-800">{{ $download->nombre_archivo }}</p><p class="mt-1 text-xs text-slate-500">{{ $download->evaluacion->codigo }} · {{ $download->usuario->name }} · {{ $download->ip_address }}</p></div><span class="text-sm text-slate-500">{{ $download->descargado_at->format('d/m/Y H:i') }}</span></div>@empty<p class="p-8 text-center text-sm text-slate-500">Todavía no se han descargado reportes.</p>@endforelse</div></x-ui.card>
</x-layouts.app>

<x-layouts.app title="Evaluaciones">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><x-ui.badge variant="info">Procesos</x-ui.badge><h1 class="mt-3 text-2xl font-bold text-navy-900">Evaluaciones</h1><p class="mt-2 text-sm text-slate-500">Procesos visibles según tus asignaciones y permisos.</p></div>@can('create',App\Models\Evaluacion::class)<a href="{{ route('admin.evaluations.create') }}" class="app-button-primary">Nueva evaluación</a>@endcan</div>
    <x-ui.card class="mt-6" :padding="false">
        <form method="GET" class="flex flex-wrap gap-3 border-b border-slate-100 p-4"><select name="estado" class="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"><option value="">Todos los estados</option>@foreach(App\Enums\EstadoEvaluacion::cases() as $state)<option value="{{ $state->value }}" @selected(request('estado')===$state->value)>{{ str($state->value)->lower()->headline() }}</option>@endforeach</select><button class="app-button-secondary">Filtrar</button></form>
        <div class="overflow-x-auto"><table class="w-full min-w-[55rem] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-3">Evaluación</th><th class="px-5 py-3">Instrumento</th><th class="px-5 py-3">Estado según cronograma</th><th class="px-5 py-3">Estructura</th><th class="px-5 py-3">Fechas</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($evaluations as $evaluation)@php($displayState = app(\App\Services\EvaluationCalendarService::class)->displayState($evaluation))<tr><td class="px-5 py-4"><p class="font-bold text-slate-800">{{ $evaluation->nombre }}</p><p class="text-xs text-slate-500">{{ $evaluation->codigo }}</p></td><td class="px-5 py-4 text-slate-600">v{{ $evaluation->modeloEvaluacion->version }}</td><td class="px-5 py-4"><x-ui.badge :variant="$displayState['variant']">{{ $displayState['label'] }}</x-ui.badge></td><td class="px-5 py-4 text-slate-500">{{ $evaluation->dominios_count }} dominios · {{ $evaluation->descriptores_count }} ítems</td><td class="px-5 py-4 text-slate-500"><span class="block">Carga: {{ $evaluation->fecha_inicio?->format('d/m/Y') ?? 'Sin fecha' }}–{{ $evaluation->fecha_limite_carga?->format('d/m/Y') ?? 'Sin fecha' }}</span><span class="mt-1 block text-xs">Revisión: {{ $evaluation->fecha_inicio_evaluacion?->format('d/m/Y') ?? 'Sin fecha' }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('evaluations.show',$evaluation) }}" class="font-semibold text-brand-700">Abrir</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No existen evaluaciones disponibles.</td></tr>@endforelse</tbody></table></div>
        @if($evaluations->hasPages())<div class="border-t border-slate-100 p-4">{{ $evaluations->links() }}</div>@endif
    </x-ui.card>
</x-layouts.app>
